"use client"; 

interface TitleProps {
    initialData: Doc<"documents">;
}

export const Title = ({
    initialData,
}: TitleProps) => {
    return ( 
        <div>
            Title
        </div>
     );
}
 
